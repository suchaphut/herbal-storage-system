# Deploy to Render

- [ ] Configure Next.js for Docker
    - [ ] Update `next.config.mjs` with `output: "standalone"`
- [ ] Create Docker Configuration
    - [ ] Create `.dockerignore`
    - [ ] Create `Dockerfile` with Node.js and Python support
- [ ] Create Render Configuration
    - [ ] Create `render.yaml` blueprint
- [ ] Verify
    - [ ] (Manual) User to test `docker build` locally or push to Render
